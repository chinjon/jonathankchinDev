<section class="hero">
    <div class="container">
        <div class="row">
            <div class="column">
                <h5>{{pageTitle}}</h5>
            </div>
        </div>
        <div class="row">
            <div class="column">
                <p>Contact Me: {{home.email}}
                    <br />
                    Tweet: {{home.twitter}}
                </p>
            </div>
        </div>
    </div>
</section>