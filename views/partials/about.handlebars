
<section class="about">
    <div class="container">
        <div class="row">
            <div class="column">
                <h2>{{pageTitle}}</h2>
            </div>
        </div>
        <div class="row">
            <div class="column column-80 column-offset-10">
                {{#each about}}
                <p>
                    {{this}}
                </p>
                {{/each}}
            </div>
        </div>
    </div>
</section>